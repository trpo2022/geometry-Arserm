#include <stdio.h>
#pragma once

void chislo(char* out, char* mass, char* judge);
void separator(
        char* inp,
        char* sample,
        char* out,
        char* x,
        char* y,
        char* r,
        char* judge);